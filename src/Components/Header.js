// src/components/Header.js
import React from "react";

/**
 * Renders the header component for the user directory app.
 * @returns {JSX.Element} The header JSX element.
 */
function Header() {
  return (
    <header className="header text-center bg-primary text-white p-3">
      <h1>User Directory</h1>
    </header>
  );
}

export default Header;
